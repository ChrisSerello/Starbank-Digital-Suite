{
  "buildCommand": "pnpm --filter @workspace/starbank run build",
  "outputDirectory": "artifacts/starbank/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ]
}